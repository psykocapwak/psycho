<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <meta name="author" content="HakimAndGeoSupinfoProject">
  <meta name="description" content="<%= yield(:meta_description)%>">
  <meta http-equiv="content-language" content="<%= I18n.locale.to_s%>">
  <title><%= content_for?(:title) ? yield(:title) : "Psykocapwak" %></title>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <script type="text/javascript">

    var _gaq = _gaq || [];
    _gaq.push(['_setAccount', 'UA-30138722-1']);
    _gaq.push(['_trackPageview']);

    (function() {
      var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
      ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
      var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
    })();

  </script>
</head>
<body>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <%= link_to 'Psykocapwak', '#', :class=>'brand'%>
        <nav>
            <div class="nav-collapse">
            <ul class="nav">
              <li>
                <%= link_to 'Accueil', root_path, :class=>"#{current_page?(:action => 'index') ? 'active' : ''}"%>
              </li>
              <li>
                <%= link_to 'Simple cap', simple_cap_path, :class=>"#{current_page?(:action => 'simple_cap') ? 'active' : ''}"%>
              </li>
              <li>
                <%= link_to 'Swim cap', swim_cap_path, :class=>"#{current_page?(:action => 'swim_cap') ? 'active' : ''}"%>
              </li>
              <li>
                <%= link_to 'Cat cap', cat_cap_path, :class=>"#{current_page?(:action => 'cat_cap') ? 'active' : ''}"%>
              </li>
            </ul>
            </div>
        </nav>
      </div>
    </div>
  </div>
  <div id="container">
    <%flash.each do |name, msg| %>
           <%= content_tag :div, msg, :id => "flash_#{name}" %>
    <%end%>
  
    <%if content_for?(:title) %>
      <h1><%= yield(:title) %></h1>
    <%end%>
  <section>
    <%= yield %>
  </section>
  </div>
  <footer>
    <strong>Psykocapwak</strong> : Un projet Supinfo et j'espère le dernier...
  </footer>
</body>
</html>
